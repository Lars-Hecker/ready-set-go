AI service using Firebase Genkit with Google AI (Gemini).

Features:
- Token quota tracking per user with monthly auto-reset
- Reserve/adjust pattern for quota enforcement
- Structured data generation with generics

Required env: `GOOGLE_API_KEY`
Optional env: `AI_DEFAULT_MODEL`, `AI_DEFAULT_QUOTA`, `AI_RESERVE_TOKENS`
